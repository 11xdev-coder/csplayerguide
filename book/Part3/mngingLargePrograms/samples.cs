namespace book.part3.mngingLargePrograms.samples;

using static System.Math;

public class samples
{
    public static void Start()
    {
        // multiple files are good
        
        // namespaces and using directives
        // namespace is a named container for organizing types
        System.Console.WriteLine("using namespace");
        double pi = PI; // without writing the name (using static)
    }
}